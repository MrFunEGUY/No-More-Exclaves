namespace = nme

# Get rid of rebels of
# effect this.solar_system.space_owner = { save_global_event_target_as = test_target }
country_event = {
    id = nme.1000
	is_triggered_only = yes	
	hide_window = yes

	trigger = {
		any_system_within_border = {
			NOR = {
				any_system_planet = {
					has_owner = yes
					owner = { is_primitive = no }
				}
				closest_system = {
				    use_bypasses = yes
				    min_steps = 1
				    max_steps = 1
				    limit = {
				        OR = {
				            NOT = { exists = space_owner }
				            space_owner = { is_same_empire = root }
				            space_owner = { NOT = { has_closed_borders = root  } }
				        }
				    }
				    exists = this
				}
			}
		}
	}
	
	immediate = {

		every_system_within_border = {
		    limit = {
		        NOR = {
		        	any_system_planet = {
                        exists = owner
                        owner = { is_primitive = no }
                    }
		            closest_system = {
		                use_bypasses = yes
		                min_steps = 1
		                max_steps = 1
		                limit = {
		                    OR = {
		                        NOT = { exists = space_owner }
		                        space_owner = { is_same_empire = root }
		                        space_owner = { NOT = { has_closed_borders = root  } }
		                    }
		                }
		                exists = this
		            }
		        }  
		    }
		    starbase = {
		        fleet = {
		            destroy_fleet = this
		        }
		    }
		    create_message = {
		    	type = NME_exclave_lost_message
		    	localization = NME_exclave_lost_message_desc
		    	days = 30
		    	target = this # the Go To
		    	variable = {
		    		type = name
		    		localization = SYSTEM
		    		scope = this
		    	}
		    }
		}
	}	
}